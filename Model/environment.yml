name: audio_env_TEST
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.6
  - numpy<2
  - scipy<1
  - ipykernel
  - ffmpeg
  - pip
  - ipython # Instalado con Conda
  - pip:
      - stempeg
      - sox
      - numba==0.48
      - librosa==0.8.1 # Puedes instalarlo explícitamente con pip
      - nussl
